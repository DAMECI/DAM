'use strict';

describe('myApp.LogIn module', function() {

  beforeEach(module('myApp.LogIn'));

  describe('view4 controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view4Ctrl = $controller('LogInCtrl');
      expect(LogInCtrl).toBeDefined();
    }));

  });
});